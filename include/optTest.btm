class Main
{
    void main()
    {
        var x = 110/(1 + 6 * 9); // x=2
        var y = 7 + x * 4; // y=15
        var out = new TextIO();
        out.putInt(x);
        if (y < 16) {
            out.putInt(y);
        }

        for (x = 0; x < 5; x++) {
            out.putInt(x);
        }
    }
}